/**
 * @NApiVersion 2.1
 * @NScriptType MapReduceScript
 */
define(['N/search', 'N/record', 'N/runtime', 'N/log'],
    function (search, record, runtime, log) {

        function getInputData() {
            const currentScript = runtime.getCurrentScript();
            const savedSearchId = currentScript.getParameter({
                name: "custscript_ada_sales_order_saved_search"
            });

            log.debug("Saved Search ID", savedSearchId);

            if (!savedSearchId) {
                log.error({ title: 'Saved search Id not provided in script parameter' });
                return [];
            }

            const loadSavedSearch = search.load({
                id: savedSearchId,
            });

            const searchCount = loadSavedSearch.runPaged().count;
             log.debug("Number of search results", searchCount);

           // log.debug("Loaded Saved Search", loadSavedSearch);
            return loadSavedSearch;
        }

        function map(context) {
            try {
                const searchResult = JSON.parse(context.value);
                const salesOrderId = searchResult.id;
                
                context.write({
                    key: salesOrderId,
                    value: searchResult
                });

            } catch (error) {
                log.error({
                    title: 'Error in map function',
                    details: error
                });
            }
        }

        function reduce(context) {
            try{
                const soDetails = context.values;
                const salesOrderId = context.key;
                log.debug("Sales Order ID", salesOrderId);
                log.debug("Sales Order Details", soDetails);
                log.debug("Sales Order Details length", soDetails.length);
                var invoiceDetailsArray = [];
                
                for(var soItemCount = 0; soItemCount < soDetails.length; soItemCount++)
                {
                    var soDetailsData = JSON.parse(soDetails[soItemCount]);
                    var createdBy = soDetailsData.values['custbody_created_by'].value;
                    var supress_email = soDetailsData.values['custbody_ada_suppress_invoice_email'];
                    var soItem = soDetailsData.values['item'].value;
                    var soItemType = soDetailsData.values['type.item'].value;
                    var soMemberItem = soDetailsData.values['memberitem.item'].value;
                    log.debug("Created By", createdBy);
                    log.debug("Suppress Email", supress_email);
                    log.debug("SO Item", soItem);
                    log.debug("SO Item Type", soItemType);
                    log.debug("SO Member Item", soMemberItem);
                    var invoiceDetails = {
                        createdBy: createdBy,
                        supress_email: supress_email,
                        item: soItem,
                        itemType: soItemType,
                        memberItem: soMemberItem
                    }
                    log.debug("Invoice Details JSON in Map",invoiceDetails);
                    invoiceDetailsArray.push(invoiceDetails);
                }
                
                log.debug("Invoice Deatails Array", invoiceDetailsArray);

                const invoiceRecord = record.transform({
                    fromType: record.Type.SALES_ORDER,
                    fromId: salesOrderId,
                    toType: record.Type.INVOICE,
                    isDynamic: true
                });

                invoiceRecord.setValue({
                    fieldId: 'custbody_created_by',
                    value: invoiceDetailsArray[0].createdBy
                });

                invoiceRecord.setValue({
                    fieldId: 'approvalstatus',
                    value: 2
                });
                log.debug("status is set to Approved");

                if (invoiceDetailsArray[0].supress_email === 'T') {
                    invoiceRecord.setValue({
                        fieldId: 'tobeemailed',
                        value: false,
                        ignoreFieldChange: true
                    });
                } else {
                    invoiceRecord.setValue({
                        fieldId: 'tobeemailed',
                        value: true,
                        ignoreFieldChange: true
                    });
                }
                var invoiceLineCount = invoiceRecord.getLineCount({sublistId: 'item'});
                for(var invLine = 0; invLine < invoiceLineCount; invLine++)
                {
                    var itemName =  invoiceRecord.getSublistValue({
                        sublistId: 'item',
                        fieldId: 'item',
                        line: invLine
                    });
                    log.debug("Item Name on Invoice Record",itemName);
                    var invItemType = invoiceRecord.getSublistValue({
                        sublistId: 'item',
                        fieldId: 'itemtype',
                        line: invLine
                    });
                    log.debug("Item Type on Invoice Record",invItemType);
                    var invItemAmount = invoiceRecord.getSublistValue({
                        sublistId: 'item',
                        fieldId: 'amount',
                        line: invLine
                    });
                    log.debug("Item Amount on Invoice Record",invItemAmount);
                    var invItemLineNo = invoiceRecord.getSublistValue({
                        sublistId: 'item',
                        fieldId: 'line',
                        line: invLine
                    });
                    log.debug("Item Line No on Invoice Record",invItemLineNo);
                    if(invItemType == 'Kit')
                    {
                        var foundItem = invoiceDetailsArray.filter(soItemName => soItemName.item === itemName);
                        log.debug("foundItem",foundItem);
                        if(foundItem && foundItem.length>0)
                        {
                            invoiceRecord.selectLine({
                                sublistId: 'item',
                                line: invLine
                            });
                            invoiceRecord.setCurrentSublistValue({
                                sublistId: 'item',
                                fieldId: 'amount',
                                value: 0
                            });
                            invoiceRecord.setCurrentSublistValue({
                                sublistId: 'item',
                                fieldId: 'custcol_ada_kit_amount',
                                value: invItemAmount
                            });
                            invoiceRecord.commitLine({
                                sublistId: 'item'
                            });
                            
                            var newKitItemAmount = (invItemAmount/foundItem.length).toFixed(2);
                            var remainingKitAmount = invItemAmount;
                            log.debug("Kit Item Amount",newKitItemAmount);
                            for(var kitLine = 0; kitLine < foundItem.length; kitLine++)
                            {
                                invoiceRecord.insertLine({
                                    sublistId: 'item',
                                    line: invItemLineNo++,
                                    ignoreRecalc: true
                                });
                                var itemName =  invoiceRecord.setCurrentSublistValue({
                                    sublistId: 'item',
                                    fieldId: 'item',
                                    value: foundItem[kitLine].memberItem
                                });
                                invoiceRecord.setCurrentSublistValue({
                                    sublistId: 'item',
                                    fieldId: 'quantity',
                                    value: 0
                                });
                                if(kitLine == (foundItem.length-1))
                                {
                                    invoiceRecord.setCurrentSublistValue({
                                        sublistId: 'item',
                                        fieldId: 'amount',
                                        value: remainingKitAmount
                                    });
                                } 
                                else
                                {
                                    invoiceRecord.setCurrentSublistValue({
                                        sublistId: 'item',
                                        fieldId: 'amount',
                                        value: newKitItemAmount
                                    });
                                    remainingKitAmount = (remainingKitAmount - newKitItemAmount)
                                }   
                                
                                invoiceRecord.setCurrentSublistValue({
                                    sublistId: 'item',
                                    fieldId: 'custcol_ada_hide_on_pdf',
                                    value: true
                                });
                                invoiceRecord.commitLine({
                                    sublistId: 'item'
                                });
                                //log.debug("New Invoice Line No : ",invLine);
                                //invoiceLineCount++;
                                //log.debug("Updated Invoice Line Count",invoiceLineCount);
                            }    

                            
                            

                        }
                    }
                }
                invoiceLineCount = invoiceRecord.getLineCount({sublistId: 'item'});
                log.debug("Final Invoice Line Count",invoiceLineCount);
                const invoiceId = invoiceRecord.save();
                log.debug("Invoice ID", invoiceId);





            }
            catch (error) {
                log.error({
                    title: 'Error in reduce function',
                    details: error
                });
            }
        }    

        function summarize(summary) {
            log.audit('Summary', {
                totalUsage: summary.usage,
                concurrency: summary.concurrency,
                yields: summary.yields
            });

            summary.mapSummary.errors.iterator().each(function (key, error) {
                log.error({
                    title: `Error for key: ${key}`,
                    details: error
                });
                return true;
            });
        }

        return {
            getInputData: getInputData,
            map: map,
            reduce: reduce,
            summarize: summarize
        };
    });Time
User
Details
Remove
1	View	Audit	Summary	11/16/2025	11:29 pm	-System-	
{
   totalUsage: 35,
   concurrency: 1,
   yields: 0
}
Remove
2	View	Debug	Invoice ID	11/16/2025	11:29 pm	-System-	1816704	Remove
3	View	Debug	Final Invoice Line Count	11/16/2025	11:29 pm	-System-	5	Remove
4	View	Debug	Item Line No on Invoice Record	11/16/2025	11:29 pm	-System-	 	Remove
5	View	Debug	Item Amount on Invoice Record	11/16/2025	11:29 pm	-System-	24.98	Remove
6	View	Debug	Item Type on Invoice Record	11/16/2025	11:29 pm	-System-	NonInvtPart	Remove
7	View	Debug	Item Name on Invoice Record	11/16/2025	11:29 pm	-System-	J024I	Remove
8	View	Debug	Kit Item Amount	11/16/2025	11:29 pm	-System-	24.98	Remove
9	View	Debug	foundItem	11/16/2025	11:29 pm	-System-	
[
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2187"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2188"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2687"
   }
]
Remove
10	View	Debug	Invoice Items Kit Array after inven item type:	11/16/2025	11:29 pm	-System-	
[
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J454BT",
      itemType: "Kit",
      memberItem: "2201"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J454BT",
      itemType: "Kit",
      memberItem: "2709"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2187"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2188"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2687"
   }
]
Remove
11	View	Debug	Item Line No on Invoice Record	11/16/2025	11:29 pm	-System-	1	Remove
12	View	Debug	Item Amount on Invoice Record	11/16/2025	11:29 pm	-System-	74.95	Remove
13	View	Debug	Item Type on Invoice Record	11/16/2025	11:29 pm	-System-	Kit	Remove
14	View	Debug	Item Name on Invoice Record	11/16/2025	11:29 pm	-System-	J024BTI	Remove
15	View	Debug	Invoice Record Line Count:	11/16/2025	11:29 pm	-System-	2	Remove
16	View	Debug	status is set to Approved	11/16/2025	11:29 pm	-System-	 	Remove
17	View	Debug	Invoice Deatails Array	11/16/2025	11:29 pm	-System-	
[
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J454BT",
      itemType: "Kit",
      memberItem: "2201"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J454BT",
      itemType: "Kit",
      memberItem: "2709"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2187"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2188"
   },
   {
      createdBy: "220718",
      supress_email: "F",
      item: "J024BTI",
      itemType: "Kit",
      memberItem: "2687"
   }
]
Remove
18	View	Debug	Invoice Details JSON in Map	11/16/2025	11:29 pm	-System-	
{
   createdBy: "220718",
   supress_email: "F",
   item: "J024BTI",
   itemType: "Kit",
   memberItem: "2687"
}
Remove
19	View	Debug	SO Member Item	11/16/2025	11:29 pm	-System-	2687	Remove
20	View	Debug	SO Item Type	11/16/2025	11:29 pm	-System-	Kit	Remove
21	View	Debug	SO Item	11/16/2025	11:29 pm	-System-	J024BTI	Remove
22	View	Debug	Suppress Email	11/16/2025	11:29 pm	-System-	F	Remove
23	View	Debug	Created By	11/16/2025	11:29 pm	-System-	220718	Remove
24	View	Debug	Invoice Details JSON in Map	11/16/2025	11:29 pm	-System-	
{
   createdBy: "220718",
   supress_email: "F",
   item: "J024BTI",
   itemType: "Kit",
   memberItem: "2188"
}
Remove
25	View	Debug	SO Member Item	11/16/2025	11:29 pm	-System-	2188
